int func1() {
    int a;
    int b;
    int c;
    set s;
    
    a = a - b / c / d - k;
    a = (a != b || 1+2 > 3) && 5.5 * 7 == 35 + func1();
    exists(b in add(1 in add( 2 in add (3 in remove(4 in s)))));
    if(f( (1 + 2) - b, 3 / (((3))), exists(b in add(1 in add(2 in s)))) && 2 > 3 || 4){
        write("oi");
        write('\n');
    }
    return a;
}