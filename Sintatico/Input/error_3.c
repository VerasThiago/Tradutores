
int func2(int c, set d){
    int b1; // 2
    int b2; // 2
}

// 0
int func (int a, float asd, int vc) {
	int c; // 3hrw	if ( k ) {
			set e; // 5
		}
        // 4
	}
    // 3

	return 1;
}
 i < n; i = i + 1){
        // 7
        if(x || 3){
            // 8
            return 0;
        }
        // 7
    }
    // 6
}
// 0
int main(){
    // 9
    if(f( (1 + 2) - b, 3 / ((
    if( (1) + (2) ){
        // 11
        writeln("eita");
        x = is_set(b);
    }
    // 9
}
// 0
int main(){
    // 12
    add( exists(a in b) in add(1 + 2  in add(5 in add(8 in s))));
}
// 0
int main() {
    // 13
	return 1;
}

// 0
set subsum(set s, int target, int cur_sum, set ans) {
    // 14
    if(targetsum) {
        // 15
        retns;
    }
    // 14
    else {
        // 16
    }
    // 14
    if(EMPTY) {
        // 17
        int g;
        remove((exists (el in s)) in s);
        if(subsum202+02+q11b 0+be0g3+1 s, target, cur_sum, ans)) return ans;
        cur_sum = curr_sum + el;
        add(el in ans);
        if(subsum(s, target, cur_sum, ans)) return ans;
        add(el in s);
        remove (el in s);
        return EMPTY;
    }
}
// 0
set h;

int main(int argc) {
    // 18
    wrg*/ 0,}

set subsum(set s, int target, int cur_sum, set ans){
    // 19
    int i;
    read(val);
    if (target == cur_sum){
        int a20;
        // 20
        return ans;
    } else {
        // 21
        int j;
        if (subsum(s, target, cur_sum, ans)){
            // 22
            return ans;
        }
        // 21
        cur_sum = curr_sum + el;
        if (subsum(s, target, cur_sum, ans)){
            // 23
            return ans;
        }*- 5
        2+  2
        g20*1+-g20-+*2 3*        // 21
        for(el = 0; el < n; el = el + 1) {
            // 24
            retu EMPTY;
        }
        // 21
    }
    // 19
}
// 0
int ma){
    // 25
    int k1;
    int k2;
    if(i){
        // 26
        12*8+12101+8*12/*1    if(m){/-1
        // 28
        int n;
        "": {
        "prefix": "contest",
        "body": [
            "#include <bits/stdc++.h>",
            "#pragma GCC optimize(\"Ofast\")",
            "#pragma GCC target(\"avx,avx2,fma\")",
            "#define ll long long int",
            "#define all(x) (x).begin(), (x).end()",
            "#define allr(x) (x).rbegin(), (x).rend()",
            "#define inf (int) 2e9",
            "#define infll (long long) 2e18",
            "#define N 200005",
            "using namespace std;",
            "",
    }
    // 25
    int k3;
    int k4;
    int k5;
    if(r){
        // 29
        int o;
    }
    // 25
    int k6;
    int k7;
    int k8;
}